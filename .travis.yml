language: node_js
before_script:
 - npm install -g gatsby-cli
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script: 
  - npm run deploy
deploy:
  provider: script
  skip_cleanup: true
  github_token: $GH_TOKEN
  on:
    branch: main